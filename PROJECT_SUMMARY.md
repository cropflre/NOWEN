# NOWEN - 星云门户

> 一个极具设计感的个人书签管理系统，采用深空美学与玻璃态设计风格

![Version](https://img.shields.io/badge/version-0.1.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Node](https://img.shields.io/badge/node-%3E%3D20-brightgreen)

---

## ✨ 功能特性

### 🏠 首页展示
- **动态时钟**：实时显示时间，带问候语切换动画
- **Aurora 极光背景**：沉浸式深空视觉效果
- **流星特效**：随机生成的流星动画
- **Spotlight 搜索**：`⌘K` 快捷键唤起，支持书签搜索和快速添加

### 📚 书签管理
- **智能元数据抓取**：自动获取网站标题、描述、图标、OG 图片
- **分类管理**：自定义分类及颜色
- **固定书签**：常用网站置顶展示（Bento Grid 非对称布局）
- **稍后阅读**：Hero 卡片展示待读内容
- **拖拽排序**：支持书签拖拽排序
- **右键菜单**：登录后支持快捷操作

### ⚙️ 后台管理
- **侧边导航**：Aceternity 风格浮动侧边栏
- **站点设置**：自定义网站名称和图标，带实时预览
- **安全设置**：密码修改，带强度指示器动画
- **数据管理**：JSON 格式导入导出备份

### 🎨 视觉设计
- **玻璃态容器**：`backdrop-blur` + 透明边框
- **Border Beam**：流光边框动画效果
- **3D 卡片**：鼠标追踪的 3D 透视效果
- **Spotlight 聚光灯**：卡片悬停光效
- **Toast 通知**：物理弹跳动画

---

## 🛠️ 技术栈

### 前端
| 技术 | 用途 |
|------|------|
| **React 18** | 核心框架 |
| **TypeScript** | 类型安全 |
| **Vite** | 构建工具 |
| **Tailwind CSS** | 原子化 CSS |
| **Framer Motion** | 动画效果 |
| **Lucide Icons** | 图标库 |
| **@dnd-kit** | 拖拽排序 |

### 后端
| 技术 | 用途 |
|------|------|
| **Express 4** | Web 框架 |
| **sql.js** | SQLite (WebAssembly) |
| **Cheerio** | HTML 解析（元数据抓取）|
| **tsx** | TypeScript 运行时 |

### 部署
| 技术 | 用途 |
|------|------|
| **Docker** | 容器化 |
| **Docker Compose** | 编排 |

---

## 📦 项目结构

```
NOWEN/
├── src/                          # 前端源码
│   ├── components/
│   │   ├── ui/                   # UI 组件库
│   │   │   ├── 3d-card.tsx       # 3D 卡片
│   │   │   ├── aurora-background.tsx  # 极光背景
│   │   │   ├── bento-grid.tsx    # Bento 网格布局
│   │   │   ├── floating-dock.tsx # 浮动 Dock 导航
│   │   │   ├── spotlight-card.tsx # 聚光灯卡片
│   │   │   ├── spotlight-search.tsx # 搜索面板
│   │   │   ├── effects.tsx       # 流星/星光特效
│   │   │   ├── advanced-effects.tsx # Border Beam 等
│   │   │   └── typewriter.tsx    # 打字机效果
│   │   ├── admin/                # 后台组件
│   │   │   ├── AdminSidebar.tsx  # 侧边导航
│   │   │   ├── SiteSettingsCard.tsx # 站点设置
│   │   │   ├── SecurityCard.tsx  # 安全设置
│   │   │   ├── DataManagementCard.tsx # 数据管理
│   │   │   └── Toast.tsx         # 通知组件
│   │   ├── AddBookmarkModal.tsx  # 添加书签弹窗
│   │   ├── AdminLogin.tsx        # 登录页
│   │   └── ContextMenu.tsx       # 右键菜单
│   ├── hooks/                    # 自定义 Hooks
│   │   ├── useBookmarkStore.ts   # 书签状态管理
│   │   ├── useTheme.ts           # 主题
│   │   └── useTime.ts            # 时间
│   ├── lib/                      # 工具库
│   │   ├── api.ts                # API 封装
│   │   └── utils.ts              # 工具函数
│   ├── pages/
│   │   └── Admin.tsx             # 后台页面
│   ├── types/
│   │   └── bookmark.ts           # 类型定义
│   └── App.tsx                   # 主应用
├── server/                       # 后端源码
│   ├── src/
│   │   ├── index.ts              # 服务入口
│   │   ├── db.ts                 # 数据库操作
│   │   └── services/
│   │       └── metadata.ts       # 元数据抓取
│   └── data/
│       └── zen-garden.db         # SQLite 数据库
├── Dockerfile                    # Docker 镜像
├── docker-compose.yml            # Docker 编排
└── package.json
```

---

## 🚀 安装部署

### 方式一：本地开发

#### 1. 克隆项目
```bash
git clone <repository-url>
cd NOWEN
```

#### 2. 安装依赖
```bash
# 安装前端依赖
npm install

# 安装后端依赖
cd server && npm install && cd ..
```

#### 3. 启动开发服务器
```bash
# 终端 1：启动后端 (端口 3001)
cd server && npm run dev

# 终端 2：启动前端 (端口 5173)
npm run dev
```

#### 4. 访问应用
- 前端：http://localhost:5173
- 后端：http://localhost:3001

#### 5. 默认管理员账号
- 用户名：`admin`
- 密码：`admin123`

---

### 方式二：Docker 部署

#### 1. 构建并启动
```bash
docker-compose up -d --build
```

#### 2. 访问应用
- 前端：http://localhost:3000
- 后端：http://localhost:3001

#### 3. 数据持久化
数据库文件存储在 `./server/data/zen-garden.db`，通过 Docker Volume 挂载实现持久化。

---

### 方式三：生产构建

```bash
# 构建前端
npm run build

# 启动后端
cd server && npm run dev

# 使用 serve 或 nginx 托管 dist 目录
npx serve dist -l 3000
```

---

## 📡 API 接口

### 书签
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/bookmarks` | 获取所有书签 |
| POST | `/api/bookmarks` | 创建书签 |
| PATCH | `/api/bookmarks/:id` | 更新书签 |
| DELETE | `/api/bookmarks/:id` | 删除书签 |
| PATCH | `/api/bookmarks/reorder` | 重排序 |

### 分类
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/categories` | 获取所有分类 |
| POST | `/api/categories` | 创建分类 |
| PATCH | `/api/categories/:id` | 更新分类 |
| DELETE | `/api/categories/:id` | 删除分类 |

### 管理员
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/login` | 登录 |
| POST | `/api/admin/logout` | 退出 |
| GET | `/api/admin/verify` | 验证 Token |
| POST | `/api/admin/change-password` | 修改密码 |

### 其他
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/metadata` | 抓取 URL 元数据 |
| GET | `/api/settings` | 获取站点设置 |
| PATCH | `/api/settings` | 更新站点设置 |
| GET | `/api/export` | 导出数据 |
| POST | `/api/import` | 导入数据 |

---

## ⌨️ 快捷键

| 快捷键 | 功能 |
|--------|------|
| `⌘/Ctrl + K` | 打开搜索面板 |
| `⌘/Ctrl + N` | 新建书签 |
| `Esc` | 关闭弹窗 |

---

## 🎯 未来计划

- [ ] 多用户支持
- [ ] 书签标签系统
- [ ] 浏览器扩展
- [ ] PWA 支持
- [ ] 更多主题配色

---

## 📄 许可证

MIT License
